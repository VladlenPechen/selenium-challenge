Requirements: 
1. JDK of a version now lower than 1.7 installed
2. Maven build tool installed and configured

How to run:
1. From a project's foot folder (the one containing POM.xml) launch cmd.exe
2. execute: mvn clean test -DsuiteXmlFile=testng.xml